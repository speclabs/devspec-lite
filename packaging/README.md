# Release packaging

The Homebrew formula and WinGet manifest are release templates. Release automation must replace `REPLACE_AT_RELEASE` with the published archive checksum and update URLs/version values before submitting to external registries.

No external registry is changed by this repository.
