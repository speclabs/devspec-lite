<protocol id="ask">
  <scope>Maintain one active material-question queue for the invoking command. A command may deliberately scope its queue to one recorded material blocker.</scope>
  <trigger>Ask one clarification for every unresolved material question in the active command queue.</trigger>
  <sequence>Ask exactly one unanswered material question at a time. Record and resolve it before asking the next material question.</sequence>
  <completion>Maintain the active material-question queue until every material question is answered or skipped. After each answer, re-evaluate every remaining material question against current decisions; skip an invalid or inapplicable material question with its reason, then ask the next valid unanswered material question. Resume the workflow only when no applicable unanswered material questions remain.</completion>
  <checkpoint>Persist material-question ID, evidence, impact, choices, recommendation, recommendation justification, applicability, canonical rule link when promoted, resume action, and answer-or-skip status in the command's decision record before waiting.</checkpoint>
  <interaction count="one" mode="interactive">
    <choices>Offer two to five meaningful exclusive choices, each with a short example.</choices>
    <recommendation required="true" justification-required="true" />
    <custom-answer required="true" />
    <fallback>Render identical text choices only when the host lacks interactive controls.</fallback>
  </interaction>
  <decision-records>
    <work-item>devspec/work-items/&lt;id&gt;/decisions.md</work-item>
    <foundation>devspec/foundation/decisions.md</foundation>
    <quickfix>Decisions section in devspec/quickfixes/QF-###-slug.md</quickfix>
  </decision-records>
  <resolution>Append the answer or skip reason to the command's decision record, update the affected artifact, then continue with one registered next action.</resolution>
</protocol>