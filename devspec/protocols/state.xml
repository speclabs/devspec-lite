<protocol id="state">
  <when>Load with every command. This is the shared vocabulary that every state record, transition, and closing report uses. devspec/lifecycle.md holds the legal route graph and devspec/glossary.md the full term list.</when>
  <repository-route>Read devspec/foundation/repository-state.md before the first foundation command. State `existing` starts at devspec.extract; state `new` starts at devspec.projectcontext. Do not infer the state from the presence of source.</repository-route>
  <run-states>`active` means the registered next command may run. `paused` is an explicit user pause. `blocked` means exactly one active material decision is recorded and the next command is devspec.clarify. `stopped` requires a new user direction before resuming. `complete` is terminal and must record `next: none`, which is itself the one reported next action.</run-states>
  <stages>Work-item stages are intake, grooming, finalization, tasks, implementation, review, and complete. A quickfix advances through triage, implementation, and validation inside its single run, then ends at complete or routed; only complete, triage, and routed appear as transition stages. Foundation and extraction work uses the foundation stage. A command that returns to its caller reports the caller's stage.</stages>
  <task-statuses>A task is pending, in-progress, blocked, complete, or superseded. A task becomes complete only after its recorded validation passes.</task-statuses>
  <evidence-labels>Label every durable fact confirmed, observed, inferred, or blocked. Never record an inferred fact as confirmed.</evidence-labels>
  <changed-work-baseline>The changed-work baseline is what an implementation is measured from: the base revision, the comparison revision or an explicit working-tree marker, and the list of changed paths. devspec.implement records it; devspec.review verifies the recorded baseline before judging the work.</changed-work-baseline>
  <resume>`return-to-caller` and `resume-origin` both mean: continue the command named in this record's saved resume reference, at its saved stage and next action. Never infer a caller. When no resume reference is saved, report the outcome and stop instead of guessing a next command.</resume>
</protocol>
