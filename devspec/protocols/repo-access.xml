<protocol id="repo-access">
  <when>Load before accessing a repository outside the scaffolding root, whenever source scope is not already confirmed, and whenever a task depends on more than one repository.</when>
  <collect>When a repository path is not already evidenced, first ask for one repository name or local path in a free-form text input. Show at least two examples such as `D:\Code\orders-api` and `D:\Code\orders-web`; allow the developer to type another path. Confirm the path before asking about access, and repeat for each additional repository.</collect>
  <validate>After each path is confirmed, ask one interactive question: “What access requirement applies to &lt;repository&gt;?” Use the named values in devspec/glossary.md#repository-access-requirements: reference-only (example: inspect architecture without edits or tests), edit (example: update an approved configuration file without running validation), edit-and-test (example: implement an approved code change and run its focused tests), validation-only (example: run an approved test suite without edits), or unavailable (example: repository is not accessible today); include Custom Answer. Recommend the least-privilege value that satisfies the current command: reference-only for extraction or discovery, edit-and-test only when approved source changes and validation are required, and validation-only for validation work. Record each repository's role, local path, workspace availability, named access requirement, and evidence. Treat the current workspace as a proposed target until an explicit user confirmation or current canonical evidence names that repository and boundary.</validate>
  <respect>Never edit a reference-only, validation-only, release-coordination, or unavailable repository. Never validate a reference-only, release-coordination, or unavailable repository. Validate a validation-only repository only when its recorded validation permission allows it.</respect>
</protocol>