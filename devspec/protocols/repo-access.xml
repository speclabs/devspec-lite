<protocol id="repo-access">
  <when>Load before accessing a repository outside the scaffolding root, whenever source scope is not already confirmed, and whenever a task depends on more than one repository.</when>
  <collect>When a repository path is not already evidenced, first ask for one repository name or local path in a free-form text input. Show at least two examples such as `D:\Code\orders-api` and `D:\Code\orders-web`; allow the developer to type another path. Confirm the path before asking about access, and repeat for each additional repository.</collect>
  <validate>After each path is confirmed, ask one interactive access question. Offer two to five exclusive choices with examples, include Custom Answer, and show exactly one recommendation and justification. Record each repository's role, local path, workspace availability, and separate read, edit, and validation permissions. Treat the current workspace as a proposed target until an explicit user confirmation or current canonical evidence names that repository and boundary.</validate>
  <respect>Never edit a reference-only, validation-only, release-coordination, or unavailable repository. Never validate a reference-only, release-coordination, or unavailable repository. Validate a validation-only repository only when its recorded validation permission allows it.</respect>
</protocol>