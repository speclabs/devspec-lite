<protocol id="repo-access">
  <when>Load before reading, editing, or validating any repository whose access is not already confirmed by current canonical evidence, and whenever the work depends on more than one repository. This includes the repository holding the devspec scaffold when that repository also holds source the command will read, edit, or validate; a scaffold-only repository needs no access requirement.</when>
  <collect>When a repository path is not already evidenced, ask for one repository name or local path at a time in a free-form text input. Show at least two examples such as `D:\Code\orders-api` and `D:\Code\orders-web`, and allow the developer to type another path. Confirm the path before asking about access, then repeat for each additional repository.</collect>
  <question>After each path is confirmed, ask one interactive question: “What access requirement applies to &lt;repository&gt;?” Offer the whole named access-requirement set as exclusive choices — reference-only, edit, edit-and-test, validation-only, release-coordination, and unavailable — taking each choice's meaning from its devspec/glossary.md entry rather than restating it here, and pairing each with a short example of when it applies, such as reading a shared library as evidence or changing one approved folder and running its focused tests. Add Custom Answer for a narrower boundary.</question>
  <recommend>Mark exactly one choice recommended and justify it. Recommend the least-privilege requirement that satisfies the current command; where normal delivery work applies that is edit-and-test, because it permits focused change and evidence. A granted requirement is capability, not authorization to change source outside the active command scope.</recommend>
  <record>Record each repository's role, local path, workspace availability, named access requirement in the lowercase form devspec/glossary.md uses, and the evidence for it, in devspec/foundation/codebase-structure.md.</record>
  <respect>Honor exactly the recorded requirement and nothing wider. Never inspect a release-coordination or unavailable repository. Never edit a reference-only, validation-only, release-coordination, or unavailable repository. Never validate a reference-only, edit, release-coordination, or unavailable repository.</respect>
</protocol>
